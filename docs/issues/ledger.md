# Eigen Fix Campaign — Master Issue Ledger

Generated from 171 scout-found + seed issues, then extended by a GUI gap-fill pass (+54 GUI issues; 221 total). Seeds marked 🌱 (tier/detail preserved). Tier substitution rule: high counts for medium/minor, medium counts for minor — never the reverse. Real bar per side: >=20 high, >=50 high+medium, >=100 total.

## APP

### High (25)

| id | tier | kind | title | file | fix |
|----|------|------|-------|------|-----|
| APP-001 | high | bug | Subtask effort/fast capping mutates a SHARED, cached provider — bleeds across parallel children and the orchestrator | `internal/agent/agent.go:subAgent/applySubtaskEffort/applySu…` | Only apply effort/fast discipline to a provider the subtask exclusively owns: skip apply* when prov == a.provider() or when prov came from the shared router cache, OR have the router hand out a fresh per-subtask provider clone. |
| APP-002 | high | bug | Live page can panic indexing d.Live[cursor] during a pending stop confirmation | `internal/app/live.go:118-165 (liveState.view) + app.go:270-…` | Call s.list.clamp() right after s.list.count = len(d.Live) in view() (and on livePollMsg in app.go), and guard the confirmStop branch with if cursor >= 0 && cursor < len(d.Live) before indexing; cancel confirmStop and show a notice if the… |
| APP-003 | high | bug | Project drill-in promises feed items but never renders them, corrupting the cursor<->row mapping | `internal/app/sessions.go:279-348 (projectsState.update/view)` | Render feed items first in view() inside-mode (loop feedFor(proj.Dir) then sessions), marking clickMap 0..feedN-1 as feed and feedN.. as sessions. Make clickAt() resolve the same way. Recompute p.feedN in view() so count, render, click, en… |
| APP-004 | high | unwired-feature | Daemon bg-task count never wired — GUI always shows 0 agents | `daemon.go (runDaemon) + internal/daemon/host.go:140 (SetBgC…` | In runDaemon (daemon.go), after constructing host, add host.SetBgCount(func() int { n := 0; for _, t := range agent.LoadBgTasks(agent.TasksDir()) { if t.Status == running { n++ } }; return n }). agent.TasksDir() is already instance-aware.… |
| APP-005 | high | unwired-feature | GUI cannot install skills (InstallFromPath/InstallFromGitHub unreachable) | `internal/gui/skills.go + frontend Skills.svelte` | Add Bridge methods InstallSkillFromPath(src) and InstallSkillFromGitHub(ref) that call skill.InstallFromPath/InstallFromGitHub with InstallOptions{Dir, Scanner, Force/Overwrite}, surfacing RiskyError reasons. Bind in bridge.ts and add an A… |
| APP-006 | high | perf | retrieve always builds an embedder pointing at localhost:8181, hammering it every Sync/Search when no embed server runs | `retrieve_run.go:38 (+ internal/llm/embed.go:50)` | Honor the bool: emb, ok := llm.NewEmbedder(); if !ok { emb = nil }. Better, only treat the embedder as present after a one-time reachability probe (or when EIGEN_EMBED_BASE_URL is explicitly set). Cache the query-embed failure. |
| APP-007 | high | unwired-feature | Custom reasoning models expose effort in the catalog but the config is never sent and effort can't be changed | `internal/llm/custom.go` | In newCustomOpenAIResponses, when cm.Reasoning attach reasoningConfig{Effort, Summary} to the payload, store effort on the adapter, and implement SetEffort/Effort (validating against cm.EffortLevels). Mirror mantle's pattern. |
| APP-008 | high | unwired-feature | Native Anthropic and Bedrock Converse never stream — the default Claude path shows no incremental output | `internal/llm/converse.go (Converse), internal/llm/anthropic…` | Implement Stream(ctx, req, sink) on both. Bedrock Converse has a converse-stream endpoint (contentBlockDelta events); native Anthropic Messages supports stream:true SSE. Reuse httpStream and add an SSE assembler mirroring parseResponsesSSE… |
| APP-009 | high | bug | Model-assisted compaction injects a synthetic user summary right before another user round, producing two consecutive user turns that Bedrock/Anthropic reject | `internal/llm/compact_summary.go:116-122 (with converse.go,…` | Make the summary a synthetic ASSISTANT turn, or merge it into recent[0]'s text the way deterministic Compact() does (recent[0].Text = injected + \n\n + recent[0].Text). Alternatively add a coalesce pass merging consecutive same-role messag… |
| APP-010 | high | unwired-feature | GUI can view but not add/remove bans — banthis layer is half-wired | `internal/gui/memory.go (no AddBan/RemoveBan bridge); Memory…` | Add Bridge.AddBan(scope, title, rule) and Bridge.RemoveBan(scope, title) delegating to the opened store, expose in bridge.ts, and add an inline add/remove affordance to the bans section in Memory.svelte. |
| APP-011 | high | bug | GUI memory save never triggers consolidation/summary (bypasses Append) | `internal/gui/memory.go:113 AppendMemory` | Change AppendMemory to call s.Append(note) (which redacts, writes the ad-hoc note, and enqueues consolidate+summary) rather than s.AddAdHocNote. |
| APP-012 | high | unwired-feature | Dreaming agent should consolidate wider chunks of work, not just eigen + last session 🌱 | `memory-dream-orientation` | internal/dream Distill/Consolidate currently scoped to eigen + last session; widen to span more sessions/projects/transcripts. |
| APP-013 | high | bug | Large MCP responses (>8 MiB line) silently kill the whole client for the session | `internal/mcp/client.go:149-176 (readLoop)` | After the loop, check sc.Err(): if bufio.ErrTooLong, surface a clear error. Raise the buffer cap (32-64 MiB) or switch to length-prefixed/streaming json.Decoder reads. Also mark lazyClient.client dead when the read loop ends so get() recon… |
| APP-014 | high | unwired-feature | Plugin HTTP/SSE (URL) MCP servers are discovered and written but the loader can never run them | `internal/mcp/load.go:16-43 (serverConfig) + internal/plugin…` | Either (a) implement an HTTP/SSE MCP transport (add URL/Type to serverConfig and a streamable-HTTP client branch in Connect/LoadTools), or (b) make the plugin installer refuse/skip URL-only MCP servers with a clear Warning instead of writi… |
| APP-015 | high | bug | Plugin hooks (auto-run shell) and MCP servers (subprocess commands) bypass the security scanner | `internal/plugin/install.go:432-437 (addHooks) and 416-430 (…` | Pass each hook command and each MCP server command through opts.Scanner before wiring, accumulating ScanFindings and honoring the RISKY-aborts-unless-Force rule. At minimum, when hooks/non-allowlisted MCP servers are present and unscanned,… |
| APP-016 | high | inconsistency | Default chat model differs between the daemon path (Opus) and the documented/in-process path (gpt-5.5) | `main.go:124-125 (flag defaults) & 523 (dc.NewSession); daem…` | Pick ONE default provider/model and use it on both paths. Either make the daemon build default to mantle/gpt-5.5 to match flag help, or change the flag default + docs to converse/opus. Simplest: pass the resolved *provider/*model when crea… |
| APP-017 | high | bug | Workflow per-step model: override runs an isolated subtask, breaking the carried-session contract | `main.go:runWorkflowHeadless (2099-2109); internal/workflow/…` | Make per-step model override carry context: send the prompt on the same carried session after a live model switch (swap the session provider, then restore), or drop model: support and document it unsupported. Never let SubtaskWith's backgr… |
| APP-018 | high | leak | Multiple un-synchronized session.Store instances write the same sessions.json concurrently | `internal/session/session.go:67 (save) + internal/gui/sessio…` | Share ONE session.Store across the GUI process (open once at startup, pass the handle into the Bridge). Have ExportSession and guiProjectDirs use that shared store. If impractical, serialize writes with a file lock (flock) around save(). |
| APP-019 | high | bug | Store.save() is a non-atomic os.WriteFile — a crash or concurrent writer truncates the whole session index | `internal/session/session.go:67` | Write to a temp file in the same dir then os.Rename over indexPath (atomic on POSIX): write sessions.json.tmp, fsync, rename. Crash-safe and gives last-writer-wins instead of corruption. |
| APP-020 | high | unwired-feature | Proactive ideas should be deduped and wider than one last session 🌱 | `skill-feed-retrieve` | feed scanSuggest / dedup is narrow (eigen + last session). Ideas should dedupe across runs and draw from a wider span of work/projects. |
| APP-021 | high | bug | apply_patch renames are completely broken (reads the non-existent destination) | `internal/tool/patch.go:applyPatch (default branch, ~line 34…` | In the default branch, read from f.oldPath (the source): data, err := os.ReadFile(resolveOldPath). Apply hunks, write to newPath, the existing os.Remove(oldResolved) finishes the move. Add a pure-rename test. |
| APP-022 | high | bug | atomicWrite destroys file permissions on every edit/write/patch | `internal/tool/fsutil.go:atomicWrite (used by edit.go, multi…` | Before the Rename, stat the destination if it exists and chmod the temp file to the original's mode (os.Stat(path) -> tmp Chmod(fi.Mode().Perm())). For brand-new files keep 0o644. |
| APP-023 | high | bug | Codex importer silently drops tool output when output is a JSON list (image/structured blocks) | `internal/transcript/codex.go` | Change Output to json.RawMessage and flatten it the way claudeResultText handles tool_result: if string use directly; if array of {type,text} blocks, concatenate text blocks. Reuse/parallel claudeResultText so all importers handle the stri… |
| APP-024 | high | bug | Rotating .bak backups are written on every Save but never read back for recovery | `internal/transcript/transcript.go` | Wire recovery into Load (or add a RecoverFromBackup helper): when the primary file is missing, zero-length, or yields zero messages while a non-empty .bak exists, fall back to the newest readable backup generation in order. |
| APP-025 | high | unwired-feature | Sessions should include other agents work (Claude Code, Codex) + resume after translating 🌱 | `transcript` | transcript readers for claude/codex/opencode/etc already exist in internal/transcript but eigen sessions do not ingest them; eigen should be able to translate an external agent transcript into an eigen session and resume it. |

### Medium (50)

| id | tier | kind | title | file | fix |
|----|------|------|-------|------|-----|
| APP-026 | medium | bug | Background task can run (and bill) up to 2x bgMaxRuntime — the cap is per-attempt, not per-task | `internal/agent/background.go:runBackgroundAttempt/SubtaskBa…` | Root all attempts of one task under a single context.WithTimeout(context.Background(), bgMaxRuntime) created once in SubtaskBackground (and promoteRunning's collector), deriving each attempt's context from it. |
| APP-027 | medium | leak | GUI AgentTranscript reads a disk path built from an unvalidated task id (path traversal) | `internal/gui/agents.go:AgentTranscript` | Export a validator from the agent package (func ValidTaskID(string) bool wrapping bgIDRe, or reuse BgRegistry.TranscriptPath) and have AgentTranscript reject ids that don't match before touching the filesystem. |
| APP-028 | medium | unwired-feature | Per-task attempt/escalation history is captured durably but never surfaced in the GUI | `internal/agent/background.go:History / taskstore.go:readTas…` | Add a Bridge.AgentHistory(id) ([]BgTaskDTO, error) that validates the id and calls agent.readTaskHistory via an exported helper, mapping to BgTaskDTO; surface it in Agents.svelte expanded view as the attempt/escalation timeline. |
| APP-029 | medium | inconsistency | Report/note truncation uses byte slicing that can corrupt multibyte UTF-8 | `internal/agent/group.go:241, groupmut.go:392, background.go…` | Replace the byte slices with rune-safe truncation — use tool.TruncateUTF8 or convert to []rune before slicing. Apply to formatGroupReport's per-child cap, oneScreen, sanitizeNote, truncateForNote. |
| APP-030 | medium | bug | List scroll window lags because update() and view() use different visible-row counts | `internal/app/list.go:28-37 (move) + per-page update/view (c…` | Compute one canonical visible-rows value from computeLayout().inner.h minus the page's fixed chrome and use it in BOTH update() and view(). At minimum replace the m.height-N literals in update() with the h-N expression view() uses. |
| APP-031 | medium | unwired-feature | Skills, Models, Providers, Observe, and Crons rows are not mouse-clickable | `internal/app/app.go:582-604 (contentClick)` | Add clickAt methods to skillsState/modelsState/providersState/cronsState (select on first click, preview/open/toggle on second) and an observeState section click, then wire the missing cases into contentClick. Add clicks.mark(lineCount(out… |
| APP-032 | medium | inconsistency | Wide-breakpoint inspector shows nothing for Live, Memory, Config, and Profile despite clear selections | `internal/app/inspector.go:55-251 (inspectorFor)` | Add inspectorFor cases: Live -> selected d.Live[cursor]; Memory -> selected bullet; Config -> fields[cursor] (key, value, type/options, Desc); Profile -> usage totals. Reuse existing detail helpers. |
| APP-033 | medium | ux | j/k do not scroll the Observe and Profile pages, leaving long content reachable only via PgDn | `internal/app/observe.go:36-40 + profile.go:23-69` | Extend handleContentScrollKey to also handle j/k as +/-1 line scroll for pages that don't own a row cursor (or map j/k to scrollContent in observeState.update). Apply to profile too. |
| APP-034 | medium | missing-state | Cumulative token / cache-hit stats reset to 0 on every daemon restart | `internal/daemon/host.go:179-228 (Stats) + session.go:158-16…` | Persist the per-session cumulative token counters in the sidecar persistMeta (host.go saveSessionMeta) and reload them in Restore() so the stats survive restart, OR relabel the GUI metric as since daemon start explicitly. |
| APP-035 | medium | unwired-feature | Eigen needs versions: know what version of daemon and gui/tui is running 🌱 | `daemon` | llm.Version=0.1.0 is a single const; daemon protocol has a Version field. Wire real versioning so daemon + gui/tui report their running version (handshake + display). |
| APP-036 | medium | bug | Pending approval is double-emitted to a view that attaches mid-wait | `internal/daemon/server.go:302-313 (attach op)` | In the attach handler, skip replay events whose Kind is approval (re-sent canonically from pendingList), or skip the pendingList re-send for approvals already present in the replay buffer. |
| APP-037 | medium | inconsistency | interrupt op reports success even when nothing was interrupted | `internal/daemon/server.go:276-282` | Have Session.interrupt() return a bool (true only when s.cancel != nil and was invoked) and surface it on the Response (e.g. an Interrupted field). |
| APP-038 | medium | perf | BM25 rank() scores every chunk against the full query on each Search (O(chunks x queryTerms)) with no posting-list shortcut | `internal/retrieve/bm25.go:152-173 + index.go:264` | Build an inverted index (term -> []chunkIdx) in buildBM25 and, in rank(), accumulate scores only over the union of posting lists for the query terms. Turns the cost from O(NxQ) into O(matching postings). |
| APP-039 | medium | missing-state | GUI Skills gallery never live-refreshes new dream proposals or out-of-band installs | `frontend Skills.svelte:55 + internal/gui/skills.go` | Add an eigen:skills event: emit after AcceptSkill/RejectSkill (and after a dream Propose if the daemon can signal), and have Skills.svelte subscribe and call load(). At minimum refresh on window focus / route re-entry. |
| APP-040 | medium | inconsistency | Propose() doc/behavior contradiction and silent overwrite of an unreviewed proposal | `internal/skill/propose.go:40-62` | Either preserve the first proposal (refuse overwrite) or stamp proposals with a modtime and surface updated state. Reconcile the comment, the map page, and behavior; consider not re-proposing an identical body. |
| APP-041 | medium | leak | RescanFeed spawns unbounded concurrent scans with no in-flight guard (cache temp-file race) | `internal/gui/feed.go:151` | Add an atomic CAS scanning guard in scanFeed so RescanFeed is a no-op (or coalesces) while a scan is in flight; OR give save()/saveSuggestCache a unique temp name. The feedLoop ticker should share the guard. |
| APP-042 | medium | bug | Suggest cache is keyed only by time, not by the project-dir set — stale suggestions for removed projects persist up to 90 min | `internal/feed/suggest.go:56-99` | Include a hash of the sorted dir set in suggestCache and treat the cache as stale when the signature changes, or drop cached items whose Dir is not in the current dirs before returning. |
| APP-043 | medium | bug | dirtyFiles miscounts the working tree when status output contains renames | `internal/feed/git.go:72-78` | Use git status --porcelain -z and count NUL-separated records (handling the rename record's embedded second path). |
| APP-044 | medium | inconsistency | Custom OpenAI-Responses and custom Anthropic providers don't stream while custom OpenAI-chat does | `internal/llm/custom.go` | Add Stream methods: customOpenAIResponses POSTs stream:true to /responses via httpStream and assembles with parseResponsesSSE; customAnthropic reuses a native Anthropic SSE assembler. Share the assembler. |
| APP-045 | medium | bug | Custom OpenAI-Responses provider doesn't retry the empty-completed-response quirk that mantle handles | `internal/llm/custom.go` | Reuse the same empty-retry loop as Mantle.Complete (post/parseReply bounded by maxEmptyRetries) in customOpenAIResponses.Complete. |
| APP-046 | medium | bug | Native Anthropic enables interleaved-thinking beta but drops thinking blocks across turns | `internal/llm/anthropic.go` | Add a thinking-block type to anthropicContent (type, thinking text, signature) plus a signature carrier on Response/Message, capture the signature on parse, and re-emit the prior thinking block (preceding tool_use) when thinking is enabled… |
| APP-047 | medium | inconsistency | Model discovery for the converse/Bedrock provider resolves AWS creds with a different profile than the converse backend actually uses | `internal/llm/discover.go:187-228` | In listBedrockModels resolve the profile the same way converse does — firstNonEmpty(EIGEN_CONVERSE_PROFILE, AWS_PROFILE, aviary) — and accept the Bedrock bearer token path. |
| APP-048 | medium | bug | catalog.Lookup prefix matching mis-resolves short/ambiguous model ids to the wrong catalog entry | `internal/llm/catalog.go:252-268` | Drop the HasPrefix(m.ID, model) direction. Keep only HasPrefix(model, m.ID) for region/version-qualified ids, and when multiple entries are prefixes of the query pick the LONGEST matching id. |
| APP-049 | medium | inconsistency | router.go doc comments claim hard general tasks are NOT routed, but Route() (and its tests) force-route them to the best tier | `internal/llm/router.go:19-35,67-83` | Update the comments to match implemented behavior (hard general routes to strongest capable tier), or add the guard: if Difficulty==DiffHard && Kind==TaskGeneral { return empty, false }. Align code, comment, and test. |
| APP-050 | medium | unwired-feature | GUI Dreaming page is read-only — no way to trigger a dream/consolidation | `internal/gui/dreaming.go (no trigger method); Dreaming.svel…` | Add a Bridge.DreamNow(scope) method that builds a memory.Pipeline with the same dream callbacks, drains the scope's queued jobs (or forces MaybeConsolidate + RegenSummary), and returns a short report. Wire a Dream now button into Dreaming.… |
| APP-051 | medium | missing-state | GUI memory/dreaming loads swallow open errors — failure looks like empty | `internal/gui/memory.go:104, internal/gui/dreaming.go:119` | Capture and return the error from memory.Open/OpenGlobal (or set an error flag on the DTO) so the frontend can render an error state. At minimum return the error so the existing Svelte catch (toasts.error) fires. |
| APP-052 | medium | bug | RegenSummary leaves a stale memory_summary.md when memory is emptied | `internal/memory/pipeline.go:404 RegenSummary` | In RegenSummary, when TrimSpace(mem) == empty, remove the summary file (os.Remove(p.Store.SummaryPath())) instead of returning silently. Optionally add a Store method to clear the summary. |
| APP-053 | medium | design | splitNotes fragments structured MEMORY.md into mismatched cards | `internal/gui/memory.go:50 splitNotes; Memory.svelte:195` | Render the curated MEMORY.md as a single Markdown document (like Summary/Bans/Profile) rather than splitting into cards, or split on top-level headings (\n## ) keeping each heading with its body. Derive NoteCount from headings or bullets. |
| APP-054 | medium | unwired-feature | USER.md should be auto-created + maintained by eigen, not hand-written by the user 🌱 | `memory-dream-orientation` | memory.UserProfile()/WriteUserProfile exist but USER.md is user-authored. Eigen should auto-create + maintain it (learned prefs), while still giving the user an extra free-form window. Memory must include USER.md. |
| APP-055 | medium | bug | Failed lazy MCP connect is never cached, so every gated tool call re-spawns and waits 15s on a broken server | `internal/mcp/load.go:150-167 (lazyClient.get)` | Cache a failure with a short cooldown (remember lastErr + a retry not before timestamp) so repeated calls fail fast for a few seconds, while still allowing eventual recovery. |
| APP-056 | medium | unwired-feature | GUI cannot enable/disable an installed plugin, though the registry, TUI, and Bubble Tea app all can | `internal/gui/plugins.go:104-112 (RemovePlugin is the only p…` | Add Bridge.SetPluginEnabled(name, enabled) delegating to reg.SetEnabled, and add an Enabled bool (via pluginEnabled logic) to InstalledPluginDTO so the page can show and toggle state. |
| APP-057 | medium | bug | MCP tool error with empty text produces a blank, useless error message | `internal/mcp/client.go:314-316 (CallToolRich)` | When res.Text is empty on an isError result, substitute a meaningful fallback, e.g. fmt.Errorf(mcp tool %q reported an error (no message), name), and include any image/other-block presence and the server name. |
| APP-058 | medium | inconsistency | Plugin agent role kind silently dropped for valid Eigen kinds (e.g. code, review) | `internal/plugin/install.go:653-660 (normalizeAgentKind)` | Source the valid-kind set from internal/agent (a single exported list/validator) instead of a private hardcoded switch, or record a Warning on the install result when a declared kind is dropped. |
| APP-059 | medium | missing-state | Plugin install reports N MCP server(s) even when some servers could not be wired (URL-only / empty command) | `internal/plugin/install.go:416-430 + discover.go:296-300` | In InstallPlugin, detect MCP servers that have neither a usable command nor supported transport, exclude them from res.Plugin.MCPServers, and emit a Warning naming each skipped server. Mirror the discoverApps len(cmd)==0 guard for discover… |
| APP-060 | medium | bug | --provider flag is silently dropped on the default daemon-backed chat path | `main.go:523 (dc.NewSession); internal/daemon/client.go:386` | Either fold --provider into the model ref before NewSession (pass llm.Ref(*provider, *model)), or add a Provider field to the daemon Request/NewSession and honor it in the build closure. Folding into the ref is the smaller change. |
| APP-061 | medium | unwired-feature | Authored workflows and custom slash-commands are reachable in the TUI but not in the GUI | `internal/gui/bridge.go (no Workflow*/Command* methods); int…` | Add bridge methods mirroring the TUI: Workflows() + RunWorkflow(sessionID, name, vars) that drives wf.Run over the daemon session, and Commands() + RunCommand(sessionID, name, args) using command.Load(command.Dirs()...).Expand(). Wire a sm… |
| APP-062 | medium | bug | Headless prompt-file/stdin task is read but never re-validated as a directory, and a directory piped in becomes a chat task | `main.go:361-392` | Document/normalize precedence: when both a positional task and piped stdin are present, either append stdin to the task or emit a one-line stderr note that stdin was ignored. Keep directory-as-root limited to the positional arg. |
| APP-063 | medium | inconsistency | telegram_token is settable via config.Set but rejected by /config <key> describe and absent from pickers | `internal/config/config.go:201-202 & 359-363; fields.go; int…` | Make the surfaces agree. Either add telegram_token to Fields() (with a free-text/secret marker so Get masks it), or remove it from Set/Get so it's truly file-only like skills_dirs. Adding a Field entry with a secret flag is cleaner. |
| APP-064 | medium | bug | Data race on *Meta fields between background titler and Discover | `internal/session/title.go:72 + internal/session/session.go:…` | In TitleUntitled, snapshot the per-session fields the worker needs into local copies, and have the worker look the meta back up by id under s.mu when it writes the title. Run -race over a concurrent Discover+TitleUntitled scenario. |
| APP-065 | medium | inconsistency | GUI ExportSession cannot export daemon-hosted sessions (app TUI can) | `internal/gui/sessions_extra.go:19` | Mirror the app's branch in ExportSession: if the id is a daemon-persisted session, transcript.Load that path and transcript.Save to dest; otherwise fall back to store.Export. |
| APP-066 | medium | missing-state | WireEvent drops Provider/Model/cache-token fields, so remote sessions lose them on the live stream | `internal/daemon/protocol.go:141 + internal/chat/remote.go:9…` | Add Provider, Model, CacheReadTokens, CacheWriteTokens to WireEvent and populate them in wireEvent(); map them back in chat/remote.go wireToEvent. Purely additive omitempty fields. |
| APP-067 | medium | bug | firstUserText reads only the first 300 lines — Codex rollouts can bury the first user message and never get titled | `internal/session/title.go:97` | Raise the line cap meaningfully for Codex (or make it source-specific), or scan until the first user message OR a byte budget is hit rather than a fixed small line count. Bump 300 to e.g. 5000. |
| APP-068 | medium | unwired-feature | Background-shell Started/Finished timestamps are dropped before the GUI/TUI | `internal/daemon/session.go:619 + protocol.go:126 + internal…` | Add Started/Finished (RFC3339 or unix) to daemon.ShellInfo and chat.ShellInfo, copy them through in session.go:619 and the local/remote chat adapters, expose in the GUI dto, and render elapsed/finished time. |
| APP-069 | medium | bug | grep/symbols/glob return ripgrep failures as successful results | `internal/tool/fsutil.go:runRipgrep + grep.go/symbols.go/glo…` | In grep/symbols/glob, treat code==2 (or any code>1) as an error: if code >= 2 { return error }. Or have runRipgrep return the exit error for codes other than 0/1. |
| APP-070 | medium | bug | todo tool parses priority but never uses it | `internal/tool/todo.go:46-77` | Either drop priority from the schema, or use it: sort/group by priority and/or include a priority marker in the rendered line. |
| APP-071 | medium | bug | Empty-message Save clobbers a good transcript with no recoverable path | `internal/transcript/transcript.go` | Guard Save against truncating an existing non-empty transcript with an empty/much-shorter message set: if len(msgs)==0 and the target already exists non-empty, refuse (return an error or no-op). |
| APP-072 | medium | missing-state | Foreign importers drop assistant Reasoning, so resumed Codex/Claude sessions lose chain-of-thought continuity | `internal/transcript/codex.go` | In parseCodex, capture reasoning response_item lines (summary text + encrypted_content) into the assistant message's Reasoning fields before flush. In parseClaude, fold thinking blocks into asst.Reasoning. |
| APP-073 | medium | leak | SaveMeta writes the meta sidecar non-atomically, risking a truncated/corrupt config on crash | `internal/transcript/meta.go` | Make SaveMeta atomic the same way Save is: write to a temp file in the same dir, Sync, Close, then os.Rename over metaPath. |
| APP-074 | medium | bug | claudeDirFromPath mis-decodes project folders containing hyphens or dotfiles, mis-grouping the Projects page | `internal/transcript/peek.go` | Decode using Claude's actual scheme: split on single -, treat -- as an escaped literal hyphen. Or only use the folder-name fallback when it resolves to an existing directory (os.Stat), otherwise leave Cwd empty. |
| APP-075 | medium | bug | claudeResultText concatenates text blocks with no separator, mashing multi-block tool results together | `internal/transcript/claude.go` | Join text blocks with a newline separator when more than one block is present, e.g. collect into []string and strings.Join(parts, \n). Mirror wherever blocks are folded. |

### Minor (34)

| id | tier | kind | title | file | fix |
|----|------|------|-------|------|-----|
| APP-076 | minor | missing-state | Empty-turn nudge is appended without persisting, unlike the reasoning-only path | `internal/agent/agent.go:1390 (drive empty-turn branch)` | Add s.persist() after appending the empty-turn nudge message, matching the reasoning-only and steer branches. |
| APP-077 | minor | bug | synthesizeReports runs outside the group's deadline | `internal/agent/group.go:174 (TaskGroup) / synthesizeReports` | Pass a context bounded by the remaining group budget to synthesizeReports (a fresh context.WithTimeout off ctx, or reuse gctx if it still has time). |
| APP-078 | minor | bug | Crons view paginates from list.top with a different height than navigation, so long timer lists scroll inconsistently | `internal/app/crons.go:206-225 (view) + 144 (update)` | Use from,to := c.list.window(visible) with the same visible value as update (after fixing the shared height mismatch), instead of the manual start:=c.list.top loop. |
| APP-079 | minor | inconsistency | Live page n (new daemon session) ignores the configured default model | `internal/app/live.go:73-82` | Pass the configured default through: d.Daemon.New(cwd, m.data.Config.Model) (fall back to empty when unset). If the daemon default is authoritative, surface that in the page copy. |
| APP-080 | minor | tech-debt | Title-bar overflow guard recomputes left to an identical value (dead branch) | `internal/app/app.go:737-739 (renderTitleBar)` | On overflow, drop the right-side action suffix first (rebuild rightText without the action) and only then strip the left purpose; or truncate(left+gap+right, title.w) explicitly. Remove the identical reassignment. |
| APP-081 | minor | tech-debt | Dead Host.Count() and redundant llm import-guard | `internal/daemon/host.go:646 (Count) + internal/daemon/sessi…` | Delete Host.Count() and remove the var _ = llm.RoleUser line — the build cannot break since llm is genuinely used in session.go. |
| APP-082 | minor | inconsistency | Stale persistMu comment claims a fixed tmp path that no longer exists | `internal/daemon/session.go:55 + host.go:259` | Update the persistMu comment to its real purpose: serialize concurrent transcript Save + .bak rotation for one session so a shutdown flush and an agent-loop Persist can't interleave writes/backups. |
| APP-083 | minor | ux | GitHub feed scan gives no signal when gh is unauthenticated vs absent — silently empty | `internal/feed/github.go:23-46` | Distinguish the cases: when gh is present but the search fails with an auth error, emit a single low-priority github Item telling the user to run gh auth login. Keep the silent no-op only when gh is genuinely absent. |
| APP-084 | minor | bug | firstSentenceAround leaks the leading - bullet marker into memory feed titles | `internal/feed/memory.go:90-103` | Strip a leading bullet marker (TrimPrefix - / TrimLeft -* \t) in firstSentenceAround before returning, or start the search after splitBullets strips it. |
| APP-085 | minor | bug | retrieve secret-file denylist misses common secret filenames | `internal/retrieve/index.go:424-439` | Broaden denied()'s basename/extension checks to cover .envrc, *.pem, *.key, id_rsa*, id_ed25519*, credentials, .netrc, *.p12, *.pfx, and normalize rel to forward slashes before the segment split. |
| APP-086 | minor | inconsistency | Codex has a doc comment for a SetSearch method that doesn't exist | `internal/llm/codex.go` | Remove the orphaned comment (codex.go:149-150). Codex correctly is not a Searcher. |
| APP-087 | minor | tech-debt | Dead methods: Converse.additionalFields, GLM.prepare, Grok.prepare are never called | `internal/llm/converse.go, glm.go, grok.go` | Delete the three unused methods (and the doc comment above additionalFields), keeping the package-level functions. |
| APP-088 | minor | bug | httpJSON silently truncates oversize responses, surfacing as a confusing JSON-decode error | `internal/llm/http.go` | Detect truncation: read with LimitReader of maxResponseBytes+1 and if the body hit the cap, return an explicit error like response exceeded NN MiB cap. |
| APP-089 | minor | bug | ShedToolImages and DedupeToolResults stubs are not recognized as already-stubbed by ShedToolResults/ShedOldToolResults, so notes get clobbered | `internal/llm/shed.go:64,157` | Extend the already-stubbed guard in both functions to also skip when Text == imagePrunedStub \|\| Text == duplicateResultStub (a small isStub helper). |
| APP-090 | minor | design | gui/routing.go ProviderDTO struct is not gofmt-clean (misaligned tags) | `internal/gui/routing.go:29-33` | Run gofmt -w internal/gui/routing.go (or align the struct fields/tags). |
| APP-091 | minor | tech-debt | listBedrockModels comment claims it lists ListFoundationModels + ListInferenceProfiles, but only queries inference-profiles | `internal/llm/discover.go:184-228` | Either add the ListFoundationModels call and merge its modelSummaries ids, or trim the comment to lists inference profiles. |
| APP-092 | minor | bug | Ban title is stored unredacted while the rule is redacted | `internal/memory/memory.go:733 AddBan` | Apply Redact to the title as well: title = strings.TrimSpace(Redact(title)) before the empty check. |
| APP-093 | minor | unwired-feature | Bridge.MemoryBackups exposed but never called by the GUI | `internal/gui/memory.go:131 MemoryBackups; bridge.ts:67` | Either wire MemoryBackups into a UI affordance (a view backups action that lets the user open/restore a snapshot) or remove the unused bridge method + bridge.ts entry. |
| APP-094 | minor | inconsistency | orientation Coupled lacks the no-history guidance the other readers give | `internal/orientation/orientation.go:1066 Coupled` | Add the same if len(eps) == 0 { print (no orientation history for this project yet); return nil } guard at the top of Coupled. |
| APP-095 | minor | inconsistency | orientation status hardcodes lagging cursors to 0 | `internal/orientation/orientation.go:1141 Status` | Either compute a real lagging-cursor count (compare cursor offsets against current source sizes) or drop the line entirely. |
| APP-096 | minor | bug | MCP server gist truncation can split a multibyte UTF-8 rune | `internal/mcp/load.go:452-456 (firstSentence)` | Truncate by runes, e.g. r := []rune(s); if len(r) > 120 { s = string(r[:117]) + ... }, matching internal/app/list.go. |
| APP-097 | minor | bug | Marketplace catalog filtering reuses the backing array via in-place slicing, risking aliasing | `internal/app/plugins.go:254-264 (filterInstalledCatalog) an…` | In filterInstalledCatalog, build a new slice (var out []PluginEntry) rather than aliasing p.catalog[:0], or guarantee no other reference exists. |
| APP-098 | minor | bug | .env parser keeps inline comments and trailing whitespace in unquoted values | `internal/config/dotenv.go:38-53` | For UNQUOTED values, strip an inline  #... comment (split on the first unescaped  #) before trimming; leave quoted values verbatim. |
| APP-099 | minor | inconsistency | Daemon/attach/remote TUI options pass raw cfg.MaxTokens while the local path resolves it | `main.go:551 vs daemon.go:397 & remote_session.go:92` | Route all three through the same resolver: pass resolveUserMaxTokens(cfg.MaxTokens) in attachTUI and remoteAttachLoop. |
| APP-100 | minor | tech-debt | Exported but unused public API: command.UserDir() and theme.NerdFont() | `internal/command/command.go:47-54; internal/theme/icons.go` | Remove command.UserDir() and theme.NerdFont(), or wire them where the duplication exists (have Dirs() call UserDir()). Lowest-risk: delete them. |
| APP-101 | minor | missing-state | GUI ObserveSummary DTO drops subagent fields the TUI app surfaces (status/promote/route/background notes) | `internal/gui/observe.go:51` | Add StatusChecks, Promotes, PromoteErrors, BackgroundNotes, RouteNotes to SubagentStatsDTO and populate from s.Subagents, then surface in the GUI observe view. |
| APP-102 | minor | inconsistency | Local status bar shows Name() (with provider suffix); Remote shows ModelID() (raw) for the same model | `internal/chat/local.go:97` | Use p.ModelID() for the tracked modelID in Local.SetModel (and in NewLocal) so both backends display the same raw id; keep ProviderName() for the provider label separately. |
| APP-103 | minor | bug | Steer-while-turn-ending race on daemon sessions can double-send a message | `internal/chat/remote.go:329 + internal/tui/switches.go:274` | Make Remote.Steer never auto-start a turn (a steer-only daemon op returning false when idle), or have steerOrQueue treat steered=false from a remote backend as already started, do not queue. Cleanest: a dedicated inject-if-running op. |
| APP-104 | minor | tech-debt | Dead code: findBlock and filePatch.renaming are unreferenced | `internal/tool/patch.go:64 (renaming) and :506 (findBlock)` | Delete findBlock entirely. Either delete renaming() or use it to drive a correct rename branch in applyPatch (which also fixes the rename bug). |
| APP-105 | minor | missing-state | memory add accepts an empty note silently | `internal/tool/memory.go:124-145` | Before Append, validate strings.TrimSpace(in.Note) != empty and return a clear error otherwise — mirroring how other tools guard required string inputs. |
| APP-106 | minor | bug | move fails across filesystems (os.Rename EXDEV) with no fallback | `internal/tool/move.go:51` | On os.Rename returning EXDEV (errors.Is(err, syscall.EXDEV)), fall back to copy-then-remove (preserving mode), handling files and directories. |
| APP-107 | minor | perf | Discover re-peeks every session full-file on a peekVersion bump despite a 256KB head cap that doesn't bound turn counting | `internal/transcript/peek.go` | Persist the turn count alongside PeekVer keyed by origin mtime so an unchanged file is not re-counted on a peekVersion bump, or cap turn counting to a sampled estimate. At minimum decouple count-version from title/cwd-version. |
| APP-108 | minor | inconsistency | peekHermes returns no cwd or title, leaving Hermes sessions ungrouped and untitled in the browser | `internal/transcript/peek.go` | Implement a real peekHermes head-scan: parse the first user line for Content and run it through titleFrom for the Title; populate Cwd if available. Bring to parity with other peekers. |
| APP-109 | minor | bug | titleFrom rejects any user message starting with { or [, blanking legitimate titles | `internal/transcript/peek.go` | Tighten the heuristic: only reject when the message parses as valid JSON (json.Valid) or looks like a complete XML/instruction tag, rather than rejecting on the first byte alone. |

## GUI

### High (16)

| id | tier | kind | title | file | fix |
|----|------|------|-------|------|-----|
| GUI-001 | high | unwired-feature | GUI scroll very slow, especially scrolling up 🌱 | `gui-components` | VirtualList recomputes the full offset array on every onscroll (unthrottled) and re-measuring not-yet-measured rows above the viewport shifts totalH/offsets, yanking the scroll anchor upward. Scrolling up is the worst case. Needs offset me… |
| GUI-002 | high | unwired-feature | Image/multimodal input is fully plumbed end-to-end but unreachable from the GUI | `internal/gui/frontend/src/lib/bridge.ts:37-40 + Chat.svelte…` | Add image intake to Composer (paste-from-clipboard + drag/drop + file picker), accumulate ImageDTO[] (mediaType + base64), and pass them through Chat.send() to Bridge.SendInput/SteerInput instead of the hardcoded []. Render thumbnails and… |
| GUI-003 | high | inconsistency | Profile Usage tokens are volatile in-memory sums, not lifetime totals | `internal/gui/frontend/src/views/Profile.svelte:71-78` | Drive the token/cache KPIs from the durable log: sum summary.models[*].inTokens/outTokens/cacheReadTokens (already fetched) for true lifetime totals. Keep daemon.stats only for live counts. Or relabel the section Live session usage. |
| GUI-004 | high | unwired-feature | Subagent/delegation telemetry computed in Go but surfaced by no view | `internal/gui/frontend/src/views/Observe.svelte (also intern…` | Add a Subagents (or Delegation) tab to Observe.svelte's tabs and render summary.subagents: KPI cards for task vs group calls, a mutating-calls line, background-done count, and error counts per kind. Same summary is already loaded — no new… |
| GUI-005 | high | ux | Approval gate hides the tool arguments — you Allow/Deny blind | `internal/gui/frontend/src/views/Chat.svelte` | Render ap.args in the approve card, e.g. a <code> block / pre under .approve__tool showing the (truncated, expandable) args string. The data is already on sess.pending[].args. Match the shell card pattern. |
| GUI-006 | high | missing-state | Chat transcript shows nothing (no loading state) while State() is in flight | `internal/gui/frontend/src/views/Chat.svelte` | Track a loading flag in the lifecycle $effect (set true before attach(), cleared in the State().then/catch) and render a transcript skeleton or Loading conversation... state when loading && sess == null && rows.length === 0, distinct from… |
| GUI-059 | high | bug | bg_done wake event spuriously flips an idle session to "working" forever | `internal/gui/frontend/src/lib/stores/transcript.sv…` | In start(), make the running guard explicit about which kinds imply an in-flight turn rather than excluding only "done": e.g. `const liveKinds = m.event.kind === "text" \|\| m.event.kind === "reasoning" \|\| m.event.kind === "tool_start" \|\| m.event.kind … |
| GUI-060 | high | bug | Reconnect re-attach re-seeds history and drops the in-flight live block, duplicating streamed content | `internal/gui/frontend/src/views/Chat.svelte` | Have transcript expose a reseed path that resets the live/pending coalescer before replacing history: in seed(), also `live = null; resetPending(); if (rafId) { cancelAnimationFrame(rafId); rafId = 0; }`. (resetPending/rafId are module-private today … |
| GUI-061 | high | missing-state | Live token counts stream in every event but the context-usage ring never updates until the turn ends | `internal/gui/frontend/src/lib/stores/transcript.sv…` | Track the latest streamed token counts in the transcript (e.g. a `liveTokens = $state(0)` updated in onEvent from e.outTokens/e.inTokens when present) and expose a getter; have Chat.svelte fold it into the displayed token count while running, or at m… |
| GUI-070 | high | perf | Tool results are stored and rendered fully uncapped — a large bash/read result floods CodeBlock with megabytes of DOM + regex | `internal/gui/frontend/src/lib/components/CodeBlock…` | Cap the rendered code in CodeBlock: if code exceeds a threshold (e.g. > ~2000 lines or ~200KB), tint+render only a head slice and show a 'show full (N more lines)' toggle mirroring DiffView's PREVIEW/expand pattern, keeping the full text only for the… |
| GUI-079 | high | missing-state | Remote-dial error gives no retry — a transient ssh failure forces close+reopen to try again | `internal/gui/frontend/src/views/Machines.svelte:15…` | Add a Retry control to the remoteError branch that re-invokes the dial for the currently-open machine, e.g. {#snippet}/inline <Button variant="secondary" onclick={() => openMachine && drill(openMachine)}>Retry</Button>. drill() already takes a Machin… |
| GUI-080 | high | unwired-feature | Remote sessions are display-only dead ends — you cannot open/attach a remote session from the drill-in | `internal/gui/frontend/src/views/Machines.svelte:16…` | Make each .rrow interactive (role=button + keydown, or wrap in the existing interactive Card pattern) and, on activate, route to a remote chat view keyed by (machine.ssh, session.id) — mirroring the TUI's remote attach. Minimum viable: add an explici… |
| GUI-081 | high | missing-state | RemoteSessions can block ~30s per dial with no cancel, and a closed sheet never aborts the ssh subprocess | `internal/gui/remote.go:47-57 (RemoteSessions) + in…` | Two parts: (1) add `-o ConnectTimeout=10` to SSHArgs (or a tighter, separate base for the read-only ListSessions peek) so an unreachable host fails fast instead of riding the 30s daemon timeout; (2) thread a context through — give Bridge.RemoteSessio… |
| GUI-091 | high | missing-state | Turn error/interrupt never clears the GUI's running state — chat wedges on "working…" forever | `internal/gui/frontend/src/lib/stores/transcript.sv…` | In transcript.svelte.ts onEvent, treat a terminal note as turn-ending: in the `note` case, after pushing the NoteBlock, set `running = false` (the daemon only emits a standalone note as the terminal event for error/interrupt; the in-turn informationa… |
| GUI-105 | high | a11y | StatusDot is always aria-hidden, so color-only session/agent/tool status is invisible to screen readers | `internal/gui/frontend/src/lib/components/StatusDot…` | Give StatusDot an optional accessible label: accept a `label?: string` prop (or derive a default per state — 'working'/'idle'/'error'/'needs approval'/'ok'), and when provided render the span with `role="img"` + `aria-label={label}` instead of `aria-… |
| GUI-106 | high | a11y | Chat transcript has no live-region semantics — streamed assistant text and tool results are never announced | `internal/gui/frontend/src/views/Chat.svelte` | Add `role="log" aria-label="Conversation" aria-live="polite" aria-relevant="additions text"` to the transcript container (or a wrapper around VirtualList). Note VirtualList only mounts visible rows, so for reliable announcement add an offscreen `aria… |

### Medium (54)

| id | tier | kind | title | file | fix |
|----|------|------|-------|------|-----|
| GUI-007 | medium | unwired-feature | GUI should display daemon + gui version (wire llm.Version through the bridge) 🌱 | `gui-bridge` | Rail footer shows 0.1.0 hardcoded-ish; wire the real daemon Version + gui build version through DaemonStats/bridge so the footer and an about surface are truthful. |
| GUI-008 | medium | missing-state | DiffView and ToolCallCard diffs have no copy affordance | `internal/gui/frontend/src/lib/components/DiffView.svelte` | Add a small sans copy control in the diffstat eyebrow (mirroring CodeBlock's copy button + 1.4s confirm + onDestroy timer cleanup) that writes the raw patch (or added lines) to navigator.clipboard. Keep it pointer/keyboard accessible. |
| GUI-009 | medium | bug | Popover never flips/clamps when the trigger sits low in the viewport | `internal/gui/frontend/src/lib/components/Popover.svelte` | In place(), compare r.bottom + panelHeight against window.innerHeight; if it would overflow, flip above the trigger (bottom: window.innerHeight - r.top + 6) or clamp top so max-height fits. |
| GUI-010 | medium | unwired-feature | Tooltip.svelte is fully built but never imported anywhere | `internal/gui/frontend/src/lib/components/Tooltip.svelte` | Wire Tooltip into the components that currently fall back to title= for explanatory hints (the truncated Badge, the Composer Stop/Send buttons, the rail footer). At minimum replace the highest-value title= usages; otherwise delete it as de… |
| GUI-011 | medium | ux | Truncated Badge loses the full value with no recoverable title | `internal/gui/frontend/src/lib/components/Badge.svelte` | Make Badge self-sufficient: when truncate is set, render the text as the element's own title (capture children text or accept an explicit title prop applied to the badge span). |
| GUI-012 | medium | a11y | CommandPalette dialog has no focus trap — Tab escapes the modal to the page behind it *(dup: also reported under gui-stores-bridge (Command palette modal focus trap))* | `internal/gui/frontend/src/lib/components/CommandPalette.sve…` | Add use:trapFocus to the .pal dialog div (import { trapFocus } from $lib/actions), matching Shortcuts/Sheet/Popover, so Tab/Shift+Tab cycle only within the palette. |
| GUI-013 | medium | a11y | CommandPalette listbox: invalid ARIA child pattern + no aria-activedescendant, so the active row isn't announced *(dup: also reported under gui-components (CommandPalette combobox + scroll-into-view); see GUI scroll-into-view item too)* | `internal/gui/frontend/src/lib/components/CommandPalette.sve…` | Give each row a stable id (id={`pal-opt-${g.index}`}), set role=combobox/aria-controls/aria-activedescendant on the input, and render the rows as non-focusable elements (div role=option, or buttons with tabindex=-1). |
| GUI-014 | medium | ux | CommandPalette never scrolls the active row into view during keyboard navigation | `internal/gui/frontend/src/lib/components/CommandPalette.sve…` | Bind each row element and in onListKey after updating active (or in an $effect tracking active) call the active row's scrollIntoView({ block: nearest }). Capture refs via bind:this into an array keyed by g.index. |
| GUI-015 | medium | unwired-feature | working toast kind is fully built but unreachable — store never exposes a working() method | `internal/gui/frontend/src/lib/stores/toasts.svelte.ts:41-51…` | Add a working: (t) => push(working, t) method (mirroring success/error/info), and wire the long-op call sites (CommandPalette.refreshFeed/pruneEmpty, etc.). If a persistent working toast is intended, let push() optionally skip the TTL time… |
| GUI-016 | medium | unwired-feature | DetachBash capability exists through the whole stack but is never callable in the GUI *(dup: also reported under gui-views-work (Detach bash control in shells panel))* | `internal/gui/frontend/src/lib/bridge.ts:62 (DetachBash) + C…` | Surface a Detach bash control in Chat — next to the working indicator while store.running, or in the session menu — calling Bridge.DetachBash(sessionId) then refreshState() so the backgrounded shell appears in the shells dock. Toast on the… |
| GUI-017 | medium | unwired-feature | MemoryBackups is half-wired: the count is shown but the backup list/restore is never reachable | `internal/gui/frontend/src/lib/bridge.ts:67 (MemoryBackups)…` | Wire a View backups expander in the Memory scope panel that calls Bridge.MemoryBackups(scope) and lists the paths (newest first), each opening its content for read/diff. At minimum make the count a button that reveals the list. |
| GUI-018 | medium | perf | Transcript seed is O(n^2) for tool-heavy sessions because daemon State carries the full uncapped history | `internal/gui/frontend/src/lib/stores/transcript.svelte.ts:1…` | Build a Map<toolId, ToolBlock> alongside out during the pass and look results up via the map (O(N)). Optionally slice the input messages to the last CAP*~2 before mapping. |
| GUI-019 | medium | unwired-feature | Memory view should surface USER.md (auto-maintained) + a user add-on window 🌱 | `gui-views-b` | GUI Memory currently shows profile edit only on global scope; surface the auto-maintained USER.md plus the additive user window. |
| GUI-020 | medium | unwired-feature | Settings (Config) dropdowns render as plain white input areas — must be designed 🌱 | `gui-views-b` | Config.svelte .select uses default/native styling reading as white inputs; restyle selects to the dark token system (bg-raised, borders, brand focus, custom chevron). |
| GUI-021 | medium | unwired-feature | Skills + Plugins pages need an Add area (accept local path OR owner/repo GitHub) 🌱 | `gui-views-b` | Add UI that accepts a filesystem path or a gh owner/repo, wired to the eigen skill/plugin install structure. |
| GUI-022 | medium | ux | Agent transcript rendered as one raw JSONL blob labeled json | `internal/gui/frontend/src/views/Agents.svelte:257` | Parse the transcript line-by-line (JSON.parse per non-empty line, tolerate bad lines) into role/content message cards in the sheet, or at minimum set lang=jsonl and pretty-print each line. |
| GUI-023 | medium | unwired-feature | Agents view hides model / kind / difficulty despite its own promise and the DTO carrying them | `internal/gui/frontend/src/views/Agents.svelte:184-210` | Render the available fields: add {#if t.model}<Badge>{t.model}</Badge>{/if} to the ag__top row, and surface kind/difficulty as a small meta line or in the transcript sheet header. At minimum show model since the comment commits to it. |
| GUI-024 | medium | unwired-feature | Bans shown as read-only raw markdown though structured per-ban data + write API exist | `internal/gui/frontend/src/views/Memory.svelte:233-241` | Add a Bridge.MemoryBans(scope) returning ListBans() as a typed array and a RemoveBan(scope, title) bridge, then render bans as a list with a per-row remove. Even read-only structured rows (title + rule) would beat the current blob. |
| GUI-025 | medium | bug | Dreaming diff sheet shows No differences when the diff actually errored | `internal/gui/frontend/src/views/Dreaming.svelte:206-213` | Add a diffError = $state<string\|null>(null) cleared at the top of openDiff and set in the catch. Render an explicit error branch in the sheet body before the empty/identical branch. |
| GUI-026 | medium | missing-state | Rollouts timeline drops the per-session timestamp that exists on disk | `internal/gui/dreaming.go:20-25 (RolloutDTO) + Dreaming.svel…` | Have RawSummaries (or a sibling method) return filename-derived timestamps; add whenMs int64 to RolloutDTO, parse the leading 20060102-150405 stamp like parseBakStamp, add whenMs to RolloutDTO in types.ts, and render relTime(r.whenMs) in t… |
| GUI-027 | medium | unwired-feature | GUI Routing view never surfaces vision or social capability, though both exist in the catalog and drive routing *(dup: also reported under gui-views-system (Routing.svelte vision chip))* | `internal/gui/routing.go:14-26,85-97 (frontend Routing.svelt…` | Add Vision bool and Social bool to ModelDTO, populate from m.Vision/m.Social, add to TS RoutingDTO, and push vision/social chips in caps() in Routing.svelte. |
| GUI-028 | medium | missing-state | Profile usage strip has no error or empty-vs-failed distinction for the log read | `internal/gui/frontend/src/views/Profile.svelte:41-50,76-77` | Track a summaryError $state set in the ObserveSummary catch; render a distinct Couldn't read the usage log note with a Retry calling load(), matching the load-failure-vs-empty pattern (commit 7c6737f). |
| GUI-029 | medium | unwired-feature | Routing model cards show single default effort, hiding the available effort range | `internal/gui/frontend/src/views/Routing.svelte:96-103` | When m.reasoning and m.effortLevels?.length, render the range (a chip effort: low-max or the level count) instead of just the default; optionally show thinkingBudget. Pure presentation change in caps(). |
| GUI-030 | medium | ux | Routing rail lists providers with zero models, leading to a dead-end empty grid | `internal/gui/frontend/src/views/Routing.svelte:113-119` | Either hide zero-count providers from the rail ({#each providers.filter(p => p.modelCount > 0)}) or disable/dim their button and skip the click handler when p.modelCount === 0. |
| GUI-031 | medium | unwired-feature | Approve action exists but is unreachable from Live/Home — approval sessions can't be resolved without opening Chat | `internal/gui/frontend/src/views/Live.svelte` | On Live (and optionally Home's live zone), for s.status === approval add an Approve affordance that opens Chat scrolled to the gate, OR fetch State + render inline Allow/Deny via Bridge.Approve. |
| GUI-032 | medium | bug | Home/Ideas feed items with only a URL still force-start a session via StartFromFeed | `internal/gui/frontend/src/views/Home.svelte` | In actOn / the card foot, when it.task is empty, hide or disable Start (only show Open); or have the button behavior key off whether a task exists. Guard StartFromFeed callers so a task-less item routes to openURL instead of creating an or… |
| GUI-033 | medium | perf | Live polls sessions.refresh() every 2s unconditionally — keeps running while daemon offline and window hidden | `internal/gui/frontend/src/views/Live.svelte` | Gate the poll on daemon.status === online (skip the refresh, keep the timer cheap, or rely on daemon.onReconnect to resume) and pause on document.hidden via a visibilitychange listener, resuming on focus. |
| GUI-034 | medium | ux | SteerInput silent fallback can drop a message into a new turn the user didn't intend | `internal/gui/frontend/src/views/Chat.svelte` | When the steer fails and you fall back to SendInput, toast something like couldn't steer — queued as a new turn so the behavior change is visible. Or surface a confirm before queueing. |
| GUI-062 | medium | bug | mapMessages matches tool results by id with a global out.find, collapsing repeated tool ids across steps into the wrong block | `internal/gui/frontend/src/lib/stores/transcript.sv…` | Mirror the live path: search backward for the most recent undone tool block with the id, and skip matching when id is empty (`if (id) { for (let i = out.length-1; i>=0; i--) ... }` else push a standalone result block). This also resolves the O(n^2) c… |
| GUI-063 | medium | bug | Steer-vs-send race: store.running can be stale at click time, sending a fresh turn the user meant to steer (or vice versa) | `internal/gui/frontend/src/views/Chat.svelte` | Don't decide steer-vs-send purely on the local running flag. Prefer a backend op that injects-if-running and reports what it did (SteerInput already returns a bool); on idle just call it and let the daemon resolve, or have send() always attempt Steer… |
| GUI-064 | medium | missing-state | No loading state on session switch: stale transcript flashes before the new session's State() resolves | `internal/gui/frontend/src/views/Chat.svelte` | Null `sess` at the top of the lifecycle $effect when the id changes (before attach), and add a loading flag set true before attach() and cleared in the State().then/catch. Render a transcript skeleton when loading && history.length === 0, and don't r… |
| GUI-065 | medium | bug | approvalSeq only ever increments, so a re-attach/replay that re-delivers an old approval event re-fires refreshState but a resolved-then-replayed approval can resurrect a stale gate | `internal/gui/frontend/src/lib/stores/transcript.sv…` | Bump approvalSeq only for live (non-replay) approval events, OR have approve() record resolved approval ids and filter them out of the rendered sess.pending list until the next authoritative State that no longer contains them. Pairs with the daemon-s… |
| GUI-066 | medium | ux | Empty-session starter chips and dock starters are gated on `online` but not on `missing`/loading, and send() swallows the gate | `internal/gui/frontend/src/views/Chat.svelte` | Derive isEmpty from the authoritative loaded state for the CURRENT id (guard with the loading flag and reset sess on id change per the loading-state fix), and have the starter chips call a send path that forces a fresh turn (never steer) since they a… |
| GUI-071 | medium | bug | Synthesized edit/write diff emits a spurious deletion (or addition) line and wrong hunk counts for empty old_string / empty content | `internal/gui/frontend/src/lib/components/ToolCallC…` | Make splitLines return [] for an empty string (e.g. `if (s === '') return []; return s.replace(/\n$/,'').split('\n');`), and compute hunk counts from the resulting array lengths. editHunk/writeDiff already iterate the arrays, so empty input then emit… |
| GUI-072 | medium | a11y | DiffView renders code as a real data <table> with no presentation role, and add/del status is conveyed only by color + aria-hidden sign gutter | `internal/gui/frontend/src/lib/components/DiffView.…` | Add role="presentation" (or role="none") to the <table> (and tr/td) so it isn't announced as tabular data, OR keep the table but expose the change kind non-visually: drop aria-hidden on the gutter for add/del rows, or add a visually-hidden prefix per… |
| GUI-073 | medium | inconsistency | ToolCallCard glyph reads green 'ok' for a done-without-result tool while the dot + note correctly read amber 'warn' | `internal/gui/frontend/src/lib/components/ToolCallC…` | Derive `tone` from the same three-way logic as dotState: error → error; done && !result → 'warn'; done && result → 'ok'; else 'running'. Add a .tool__glyph--warn { color: var(--warn); } rule so the glyph agrees with the dot and the amber note. |
| GUI-074 | medium | bug | Multi-line raw HTML block from the model is collapsed onto one line (newlines lost) in Markdown | `internal/gui/frontend/src/lib/components/Markdown.…` | Render raw HTML-block text in a whitespace-preserving element instead of a <p>: e.g. `<pre class="md-rawhtml">{text}</pre>` (or a div with white-space: pre-wrap), so multi-line literal HTML reads as written. Optionally skip emitting anything for toke… |
| GUI-075 | medium | perf | DiffView recomputes additions/deletions/hunks by filtering the full rows array three separate times on every reactive pass | `internal/gui/frontend/src/lib/components/DiffView.…` | Tally counts inside the single `rows` $derived parse loop (increment local add/del/hunk counters as each Row is classified) and return them alongside rows (e.g. return { rows, additions, deletions, hunks }), or compute all three in one $derived.by th… |
| GUI-082 | medium | bug | Card #each key is m.name, but saved + ssh-config can collide / be blank, risking duplicate-key and mis-render | `internal/gui/frontend/src/views/Machines.svelte:11…` | Key on a stable composite that can't be empty, e.g. (m.name \|\| m.ssh) or `${m.name}\|${m.ssh}`. In the card, fall back the displayed name to m.ssh when m.name is empty so a malformed/blank entry is still identifiable. Optionally have remote.Machines()… |
| GUI-083 | medium | missing-state | Machines list never refreshes after a CLI `remote add` / ssh_config edit — load() runs once on mount | `internal/gui/frontend/src/views/Machines.svelte:46…` | Add a lightweight refresh: re-call load() on window focus / route re-entry (matching the pattern suggested for Skills in APP-039), and/or add a small Refresh affordance in the header next to the count. Machines() is instant + local (no ssh), so refre… |
| GUI-084 | medium | missing-state | Bridge.Machines() is typed MachinesDTO\|null but a null resolution masquerades as 'No machines yet' | `internal/gui/frontend/src/views/Machines.svelte:38…` | Treat a null resolution as a load failure: in load(), if (seq===loadSeq) { if (d) data = d; else error = 'Could not load machines'; }. Or tighten the bridge type to Promise<MachinesDTO> since the Go method always returns a non-nil DTO on success, and… |
| GUI-085 | medium | ux | Detected-only hosts show a misleading install hint and no signal that they may not run eigen at all | `internal/gui/frontend/src/views/Machines.svelte:15…` | Soften the detected-only path: in the remote-error hint, when the machine is detected-only (!m.saved), phrase it as 'This ssh host may not run eigen' rather than commanding an install, and/or gate the install hint behind m.saved. Consider visually se… |
| GUI-086 | medium | bug | base() dir helper collapses distinct remote dirs and mishandles root/trailing-slash paths | `internal/gui/frontend/src/views/Machines.svelte:79…` | Keep the basename for the primary label but make the full dir reachable without a tooltip — render it in a secondary muted line (as the card's .mc__mono dir does) or make the row expandable. At minimum normalize backslashes and handle empty/root by s… |
| GUI-087 | medium | perf | RemoteSessions opens a fresh ssh process per drill with no connection reuse or in-flight coalescing | `internal/remote/dial.go:77-97 (ListSessions: Dial …` | At the Go seam, optionally cache a short-lived ssh connection per target for re-list within a few seconds, or at least coalesce concurrent ListSessions(target) calls. At the view, debounce/guard drill() so re-dialing the already-open machine while re… |
| GUI-092 | medium | bug | Non-streaming providers' final answer is dropped from the live stream (EventDone.text discarded) | `internal/gui/frontend/src/lib/stores/transcript.sv…` | In the transcript store `done` case, when e.text is non-empty and no live/pending text for this step has been shown (i.e. the provider didn't stream), push a final text block: `if (e.text && !streamedThisTurn) pushHistory({uid: nextUid(), kind:'text'… |
| GUI-093 | medium | ux | Error notes render identically to benign notes — failures look like ordinary status lines | `internal/gui/frontend/src/views/Chat.svelte:376-37…` | Carry the error/severity through the event contract instead of relying on a string prefix: add an EventError kind (or reuse IsError on the note WireEvent) in finishTurn for the err!=nil branch, mirror it in WireEventDTO, and add a NoteBlock `tone` ('… |
| GUI-094 | medium | missing-state | WireEventDTO drops provider/model/cache-token fields the agent sets on EventDone — GUI can't show per-turn provider/model or cache hit-rate live | `internal/gui/dto.go:51-62 (WireEventDTO) + interna…` | After APP-066 adds Provider/Model/CacheReadTokens/CacheWriteTokens to daemon.WireEvent, add the same omitempty fields to WireEventDTO in dto.go (mapped in toWireEventDTO), mirror them in types.ts WireEventDTO, and surface provider/model + cache-hit o… |
| GUI-097 | medium | unwired-feature | Plugin scan findings (what was flagged on a forced install) are dropped at the DTO — GUI shows 'scan: forced' but never why | `internal/gui/plugins.go:16-30,61-70 + internal/gui…` | Add `Scans []ScanFindingDTO` (Component string; Reasons []string) to InstalledPluginDTO and populate it from p.Scans in Plugins(). In Plugins.svelte, when scanStatus === 'forced', render an expandable detail under the row listing each Component and i… |
| GUI-098 | medium | missing-state | Config select fields (model / judge_model) can never be cleared back to empty/'automatic' from the GUI | `internal/gui/frontend/src/views/Config.svelte:147-…` | For dynamic-option fields that accept empty (judge_model, and arguably model), prepend an explicit option like `<option value="">(automatic)</option>` to the select, or render these as a combobox/free-text-with-suggestions so an empty/unset value is … |
| GUI-099 | medium | ux | Crons collapses four distinct timer states into one 'active = next>0' flag, so Stop/Disable give no visible result | `internal/gui/crons.go:62-70 + internal/gui/fronten…` | Surface real state: have loadSystemdTimers also read each unit's enabled state (`systemctl --user is-enabled <unit>`) and active/running state, add `Enabled bool` (and keep Active for running) to CronDTO. In Crons.svelte show Enable/Disable driven by… |
| GUI-100 | medium | missing-state | Plugins view never refreshes after out-of-band installs/uninstalls and leaves stale inline-confirm state across reloads | `internal/gui/frontend/src/views/Plugins.svelte:25-…` | Subscribe to a plugins-changed signal (emit an `eigen:plugins` event after RemovePlugin/SetMarketEnabled/RemoveMarketplace, and ideally after a CLI install if the daemon can signal) and call load() on it; at minimum re-load on window focus / route re… |
| GUI-107 | medium | a11y | Markdown task-list checkboxes are aria-hidden with no text alternative — checked/unchecked is color-only | `internal/gui/frontend/src/lib/components/Markdown.…` | Replace the decorative span with semantic state: render a real `<input type="checkbox" checked={item.checked} disabled>` (native checkbox is announced as checked/unchecked), or keep the styled span but add `role="checkbox" aria-checked={!!item.checke… |
| GUI-108 | medium | a11y | Config boolean toggle and route_providers chip controls have no reduced-motion guard | `internal/gui/frontend/src/views/Config.svelte` | Extend the reduced-motion block to add `.toggle__knob, .toggle, .chip { transition: none; }` (and `.toggle--on .toggle__knob { transform: translateX(18px); }` is fine as a static end-state). Mirrors how Live/Home/Card hold their end-states statically… |
| GUI-109 | medium | a11y | Hand-rolled modal scrims (role=button) only handle Enter, not Space — and several aren't reachable as the spec implies | `internal/gui/frontend/src/lib/components/Sheet.sve…` | Either (a) drop the role=button/keydown from the scrims entirely (they're decorative click-catchers; Escape already closes via the dialog's handler and trapFocus keeps focus inside), leaving just `onclick`; or (b) if kept as buttons, handle Space too… |

### Minor (42)

| id | tier | kind | title | file | fix |
|----|------|------|-------|------|-----|
| GUI-035 | minor | inconsistency | Composer comment claims MAX_ROWS is read from a CSS custom property, but it is a JS const | `internal/gui/frontend/src/lib/components/Composer.svelte` | Either define --composer-max-rows: 8 in the component CSS and have grow() read it via getComputedStyle, or correct the comment to state the cap is a JS constant. |
| GUI-036 | minor | ux | EmptyState glyph is decorative-only with no semantic mapping to its meaning | `internal/gui/frontend/src/lib/components/EmptyState.svelte` | Either accept a headingLevel prop (default 2) so callers nesting an EmptyState inside a section can set the correct level, or render the title as a non-heading element with role=heading aria-level driven by context. |
| GUI-037 | minor | bug | ToastHost reduceMotion is captured once at module load and never reacts to OS changes | `internal/gui/frontend/src/lib/components/ToastHost.svelte` | Make it reactive: store the MediaQueryList, subscribe to its change event (in an $effect with teardown), and update a $state flag so enterMs/exitMs recompute. |
| GUI-038 | minor | a11y | App.svelte route-change fly transition ignores prefers-reduced-motion | `internal/gui/frontend/src/App.svelte:52` | Compute a reduceMotion flag (matchMedia(prefers-reduced-motion: reduce).matches) and use duration: reduceMotion ? 0 : 180 in the in:fly options. |
| GUI-039 | minor | inconsistency | CommandPalette and Shortcuts overlays can stack, and a single Escape is handled by both | `internal/gui/frontend/src/lib/components/CommandPalette.sve…` | Coordinate the two global overlays — a tiny shared overlay store (or have each opener close the other), and align z-indexes so the focused/most-recent overlay is on top. At minimum have the palette's show() dismiss the Shortcuts sheet and… |
| GUI-040 | minor | inconsistency | Shortcuts cheatsheet is incomplete/misleading — duplicate Enter rows, omits palette actions and toast dismissal | `internal/gui/frontend/src/lib/components/Shortcuts.svelte:1…` | Group the two Enter rows under a context column or merge with clearer scoping (Enter — open selection / send message in Chat), and add a short line pointing at the palette's verb set. |
| GUI-041 | minor | ux | TopBar title shows the raw route id, never the session/context — chat instead of the session title | `internal/gui/frontend/src/lib/components/TopBar.svelte:33` | Derive a display title: when route===chat and router.param is set, look up sessions.list for that id and show its title (fallback to Chat); otherwise keep the capitalized route. Optionally centralize a route->label map in router.svelte.ts. |
| GUI-042 | minor | tech-debt | Bridge.Stats() is unused and typed Promise<unknown> while a typed DaemonStats already exists | `internal/gui/frontend/src/lib/bridge.ts:28 (Stats)` | If kept (for an explicit refresh), type it as Promise<DaemonStats \| null>; if redundant with the push stream, remove the wrapper to keep bridge.ts a 1:1 surface. |
| GUI-043 | minor | unwired-feature | FeedFor (per-project feed drill-in) is bridged but never used | `internal/gui/frontend/src/lib/bridge.ts:98 (FeedFor)` | Either wire FeedFor into a per-project view (expanding a Home project row, or a project header in Sessions/Chat to show that dir's loose ends) or drop the FeedFor wrapper from bridge.ts and the Go method. |
| GUI-044 | minor | inconsistency | SessionInfoDTO.updated is unix-nano but undocumented at the seam, and the nano->ms rel() is copy-pasted across three views | `internal/gui/frontend/src/lib/types.ts:43 (updated)` | Comment updated as // unix nano in types.ts, and extract the shared nano->relative-time helper into lib (a relTime() in status.ts or a time util) so the three views call one source of truth. |
| GUI-045 | minor | inconsistency | types.ts DaemonStats drifts from dto.go: daemon identity/version fields are missing | `internal/gui/frontend/src/lib/types.ts:78-95 (DaemonStats)` | Add version?, executable?, binary_sha256?, vcs_revision?, vcs_modified? to the DaemonStats type and surface version (and optionally short binary_sha256/vcs_revision) in the Observe runtime panel. |
| GUI-046 | minor | ux | Memory Add note never focuses the textarea it reveals | `internal/gui/frontend/src/views/Memory.svelte:122-135,151-1…` | Bind the textarea and focus it when composing flips true (an $effect that focuses on mount, or use:autofocus action). |
| GUI-047 | minor | inconsistency | Memory note + profile textareas have no keyboard submit, unlike Chat's goal editor | `internal/gui/frontend/src/views/Memory.svelte:128-143,219-2…` | Add onkeydown={(e) => { if (e.key === Enter && (e.metaKey \|\| e.ctrlKey)) { e.preventDefault(); saveNote(); } }} to the note textarea (and saveProfile to the profile textarea). |
| GUI-048 | minor | missing-state | Memory shows No memory yet empty state even when backups/consolidations exist | `internal/gui/frontend/src/views/Memory.svelte:149-154` | Include backups in the non-empty test, or change the empty-state copy/CTA to acknowledge prior history (link to Dreaming) when current.backups > 0. |
| GUI-049 | minor | ux | Skills paging reveals in discovery order then re-buckets, so Show more injects cards into earlier shelves | `internal/gui/frontend/src/views/Skills.svelte:77-98,249-255` | Page after grouping rather than before: compute full shelves from all filtered skills, then cap the total rendered cards across shelves in order (or cap per shelf). |
| GUI-050 | minor | a11y | role=tab segmented controls lack arrow-key navigation and tabpanel association | `internal/gui/frontend/src/views/Memory.svelte:96-117 + Drea…` | Either add the standard tab keyboard pattern (roving tabindex + Arrow handler, aria-controls pointing at a role=tabpanel) or downgrade to role=group with aria-pressed toggle buttons so announced semantics match behavior. |
| GUI-051 | minor | bug | splitNotes keeps the leading heading its comment claims to drop, so the first note card is a stray # Memory line | `internal/gui/memory.go:48-67` | Either implement the documented behavior (skip a first chunk that is solely a top-level ATX heading) or fix the comment. Implementing the drop is better since the heading is structural. |
| GUI-052 | minor | ux | Config select shows no saving feedback while a commit is in flight | `internal/gui/frontend/src/views/Config.svelte:121-173` | Add a lightweight inline saving... affordance per row when saving[f.key] is true (a small spinner or dimmed label next to the control). |
| GUI-053 | minor | ux | Crons crontab decoder ignores day-of-month/month combos and silently shows raw spec | `internal/gui/frontend/src/views/Crons.svelte:130-164` | Either expand cadence() to handle ranges/lists/steps in dow/month and day-of-month, or when it can't decode still label it (custom schedule) above the verbatim spec rather than duplicating the raw glob as both cadence and spec. |
| GUI-054 | minor | inconsistency | Plugins exposes rich DTO fields (installedMs, scanCount, marketplace owner/addedMs) that no row renders | `internal/gui/frontend/src/views/Plugins.svelte:86-99,126-198` | Show install date (rel-time from installedMs) and scanCount next to the scan badge on plugin rows, and owner/added on marketplace rows — small additions reusing the existing rel-time formatter. |
| GUI-055 | minor | ux | Chat near-limit compact nudge and Compact menu item give no feedback while compacting | `internal/gui/frontend/src/views/Chat.svelte` | Swap the nudge label to compacting... (or add a spinner) while compacting, and on the menu Compact item show loading state. The compacting $state already exists. |
| GUI-056 | minor | inconsistency | Home relative-time labels are frozen — not tied to the shared clock like Live/Sessions | `internal/gui/frontend/src/views/Home.svelte` | Import { now } from $lib/stores/clock.svelte and add void now.ms; at the top of Home's rel() (matching Live/Sessions). |
| GUI-057 | minor | a11y | Home stat strip and live/resume rows are <button>s with no accessible name | `internal/gui/frontend/src/views/Home.svelte` | Add aria-label to the strip button (Open Observe — telemetry) and to .lr/.row buttons (aria-label={`Open session ${s.title \|\| untitled}`}). title alone is insufficient for accname. |
| GUI-058 | minor | inconsistency | Sessions Export has no daemon-offline / disabled guard while other actions implicitly require online | `internal/gui/frontend/src/views/Sessions.svelte` | Either disable mutating buttons (Export/Delete/Prune) when daemon.status !== online with a title explaining why, or show a thin offline banner on these views, matching Chat's online gating. |
| GUI-067 | minor | a11y | Composer Enter-to-send has no keyboard escape hatch and the disabled-reason is only conveyed visually/title | `internal/gui/frontend/src/lib/components/Composer.…` | Give the textarea aria-describedby pointing at a (non-aria-hidden) status element carrying the disabledReason when disabled, and surface the offline reason via the Button's accessible name rather than only title. Drop aria-hidden from the hint or dup… |
| GUI-068 | minor | ux | Stop/Interrupt gives no in-flight feedback and can be clicked repeatedly with no debounce | `internal/gui/frontend/src/views/Chat.svelte` | Add an `interrupting` $state set on click and cleared on the next done/running=false (or RPC resolve), disable the Stop button while interrupting, and toast/label "stopping…". Optionally surface the daemon's interrupted-or-not result once APP-037 lan… |
| GUI-069 | minor | perf | rows derived array spreads the full history on every live-block frame during streaming | `internal/gui/frontend/src/views/Chat.svelte` | Avoid spreading the whole history every frame: render history and the live block as two adjacent lists (history via VirtualList, the single live block appended outside the windowed list or as a fixed trailing row), or feed VirtualList a stable items … |
| GUI-076 | minor | a11y | CodeBlock copy button has CSS transitions but no prefers-reduced-motion guard, unlike every sibling rich component | `internal/gui/frontend/src/lib/components/CodeBlock…` | Add `@media (prefers-reduced-motion: reduce) { .code__copy { transition: none; } }` at the end of CodeBlock's <style>, matching the pattern in DiffView/ToolCallCard/Markdown. |
| GUI-077 | minor | ux | CodeBlock copy button gives no feedback when the result is empty and silently no-ops when clipboard is denied | `internal/gui/frontend/src/lib/components/CodeBlock…` | On the catch branch, surface a brief failed state (e.g. set a `failed` flag that shows '✕ copy failed' for ~1.4s) so the click isn't silent; and/or disable/hide the copy button when code.trim() is empty so the success checkmark never lies about copyi… |
| GUI-078 | minor | ux | Collapsed large DiffView cannot horizontally scroll its preview because the clipped container sets overflow:hidden | `internal/gui/frontend/src/lib/components/DiffView.…` | Use overflow-x:auto; overflow-y:hidden in .diff__scroll--clipped (clip only the vertical for the fade) so wide preview lines remain horizontally scrollable while collapsed. |
| GUI-088 | minor | a11y | Dialing spinner conveys progress only visually — no aria-live / aria-busy for screen readers | `internal/gui/frontend/src/views/Machines.svelte:15…` | Wrap the dialing/result region in role="status" aria-live="polite" (and aria-busy={remoteLoading} on the body) so the 'Dialing over ssh…' text and the resolved outcome are announced. The reduced-motion fallback for the spinner already exists; only th… |
| GUI-089 | minor | design | Card title tooltip says 'Dial … for live sessions' even when the host likely has no eigen daemon | `internal/gui/frontend/src/views/Machines.svelte:11…` | Make the title conditional: for saved hosts keep 'Dial … for live sessions'; for detected-only hosts use 'Check <ssh> for an eigen daemon' or similar so the affordance's promise matches the likely outcome. |
| GUI-090 | minor | missing-state | No daemon-offline guard on the Machines view despite the rest of the GUI gating on daemon status | `internal/gui/frontend/src/views/Machines.svelte (n…` | Low effort: a one-line muted note in the header (or in the sheet) clarifying that dialing reaches the remote host directly over ssh and works regardless of the local daemon. No functional change needed; this is purely orientation copy to prevent user… |
| GUI-095 | minor | tech-debt | fromMessageDTOs round-trip is dead in production (only exercised by tests) yet asymmetrically drops reasoning identity fields | `internal/gui/dto.go:160-181 (fromMessageDTOs) + in…` | Delete fromMessageDTOs (and fromImageDTOs' message-level use) and its test if no Bridge method needs the TS->Go message direction; the seam is read-only for history. If a future feature does need it (e.g. importing a transcript), first add ReasoningI… |
| GUI-096 | minor | inconsistency | daemon DaemonStats is emitted raw (un-DTO'd) on the stats event, so types.ts must mirror Go json tags exactly with no compile-time guard | `internal/gui/bridge.go:106 (emit eventDaemonStats,…` | Either introduce a DaemonStatsDTO with the project's camelCase convention and a toDaemonStatsDTO mapper (consistent with the rest of dto.go, and a single place to keep parity), or at minimum document in types.ts that DaemonStats mirrors daemon.Daemon… |
| GUI-101 | minor | inconsistency | config.Set never validates the 'effort' closed-set the GUI advertises, so the select silently trusts an unenforced contract | `internal/config/config.go:179-180 + internal/confi…` | Add validation to the effort case in config.Set mirroring the other closed fields: reject values not in the Fields() option set (allow empty for 'unset'), returning a clear error. This makes the closed-set contract real and keeps the GUI select and b… |
| GUI-102 | minor | ux | Config toggle and multi-select chips give no in-flight feedback and chips show no optimistic state | `internal/gui/frontend/src/views/Config.svelte:80-8…` | Show the same lightweight 'saving…'/dimmed affordance for the toggle and chips when saving[f.key]. For chips, set values[f.key] to the optimistic joined set before awaiting commit (commit already reverts on error), so the chip reflects the pending to… |
| GUI-103 | minor | a11y | Skills proposal review strip is a horizontal scroll region unreachable by keyboard | `internal/gui/frontend/src/views/Skills.svelte:195-…` | Make the scroll region keyboard-operable: add `tabindex="0"` + a role/label to .strip__row (or handle ArrowLeft/Right to scroll), and on focus of a proposal's button call scrollIntoView({block:'nearest', inline:'nearest'}) so tabbing through Accept/R… |
| GUI-104 | minor | ux | Plugin Uninstall and Marketplace Remove fire on a single inline confirm with no consequence preview, despite being fully destructive | `internal/gui/frontend/src/views/Plugins.svelte:146…` | In the confirm state for a plugin, replace the bare 'Uninstall?' with a one-line consequence summary built from components(p) (e.g. 'Remove 3 skills, 2 agents, 1 mcp?'). Keep the marketplace confirm lighter-weight so the visual weight matches the act… |
| GUI-110 | minor | a11y | Home feed-card dismiss (×) icon button has an aria-label but no visible focus indicator | `internal/gui/frontend/src/views/Home.svelte` | Add `.fc__x:focus-visible { outline: none; box-shadow: var(--shadow-focus); color: var(--text-primary); }` mirroring the other focus-visible treatments in the codebase. |
| GUI-111 | minor | a11y | Skills proposal hover/scroll transitions lack a reduced-motion guard | `internal/gui/frontend/src/views/Skills.svelte` | Add `.sk__rail, .prop--more { transition: none; }` to the existing `@media (prefers-reduced-motion: reduce)` block. |
| GUI-112 | minor | a11y | Memory and Routing segmented/provider controls have unguarded color transitions under reduced motion | `internal/gui/frontend/src/views/Memory.svelte` | Add the segmented/rail/search transitions to each view's reduced-motion block, e.g. Memory `.mem__scope { transition: none; }` and Routing `.prov, .route__search { transition: none; }`. |

## Counts

### APP

- high: **25** (bar: >=20) ✅
- medium: **50**
- minor: **34**
- high+medium combined: **75** (bar: >=50) ✅
- total: **109** (bar: >=100) ✅


### GUI

- high: **16** (bar: >=20) ❌
- medium: **54**
- minor: **42**
- high+medium combined: **70** (bar: >=50) ✅
- total: **112** (bar: >=100) ✅


## Gaps

### APP

Bar met. No additional issues required.

### GUI

- HIGH+MEDIUM combined bar met: **70** (>=50) ✅
- TOTAL bar met: **112** (>=100) ✅
- still need **4** more HIGH (16 -> 20) to clear the high floor — the only remaining GUI gap

### GUI — where the remaining 4 HIGH issues likely live (follow-up scout directions)

The gap-fill pass closed the total and high+medium bars and broadened coverage into the rich-components
(CodeBlock/DiffView/ToolCallCard/Markdown), the machines-remote stack (`Machines.svelte` + `remote.go` +
`internal/remote/*`), the pump/DTO parity seam, the install-flows views, and a cross-app a11y/reduced-motion
sweep. Only the >=20 HIGH floor remains (4 short). Highest-odds places for genuine HIGH-tier defects still:

1. **`Chat.svelte` deep transcript/streaming behavior + `transcript.svelte.ts`** — still the densest area.
   Beyond the bg_done / reconnect-reseed / stuck-running / live-token items now logged: incremental-append vs
   full-reseed correctness on rapid turns, reasoning-delta ordering vs text, tool-call card collapse/expand
   state across reconnect, scroll-anchor during live streaming (ties to the VirtualList seed GUI-001).
2. **`VirtualList.svelte`** — only the scroll-perf seed (GUI-001) and the per-frame spread (GUI-069) are logged.
   ResizeObserver / dynamic-row remeasure correctness, key churn on reseed, and anchor preservation while
   prepending history are unexamined and HIGH-likely.
3. **`internal/gui/bridge.go` + `dto.go` + `pump.go` (event pump / RPC seam)** — reconnect/replay ordering,
   error propagation across the bridge, and a full DTO<->types.ts field-parity sweep across every bridge method
   (most still un-audited individually) can surface HIGH correctness/data-loss issues like the WireEvent drops.
4. **Remote attach end-to-end** (depends on wiring GUI-080) — once remote sessions are actionable, the remote
   chat/attach lifecycle (auth, version mismatch, mid-attach disconnect) is fresh HIGH-tier territory.

A focused follow-up scout on #1–#3 should close the remaining 4-high gap honestly.

