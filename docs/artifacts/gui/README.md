# GUI desktop screenshot artifacts

These PNGs are copied from isolated X11 workspace captures used during the GUI phase.

- `release-app-shell.png` — release binary app shell rendered in xterm/tmux inside the isolated desktop workspace.
- `chat-tui-shell.png` — smoke-tagged chat TUI rendered in xterm/tmux inside the isolated desktop workspace.

The visual assertions for these artifacts are documented in `../../gui-screenshot-artifacts.md`.
